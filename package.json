{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Multi-user to-do web app with username/password login and per-user task lists",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --env-file-if-exists=.env --test"
  },
  "keywords": [
    "todo",
    "express",
    "sqlite"
  ],
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2",
    "express-session": "^1.18.1"
  }
}
